[package]
name = "rust-cid"
version = "0.1.0"
authors = ["Aten <jincxmain@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thiserror = "1.0"
integer-encoding = "1.0.7"

multihash = { package = "parity-multihash", git = "https://github.com/libp2p/rust-libp2p" }

multibase = { package = "rust-multibase", path = "../multibase" }
